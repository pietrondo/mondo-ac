schema: spec-driven

context: |
  Tech stack: Vite, Three.js, TypeScript, lil-gui
  Architecture: 3D Web Application / Game Canvas
  Testing: Vitest for Unit/Integration, Playwright for E2E
  Style: Vanilla CSS, TypeScript strict checks

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns
    tdd: true
    test_command: "npx vitest run"
  verify:
    test_command: "npx vitest run"
    build_command: "npm run build"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas

testing:
  strict_tdd: true
  detected_at: "2026-07-14"
  test_runner:
    command: "npx vitest run"
    framework: "Vitest"
  layers:
    unit:
      available: true
      tool: "Vitest"
    integration:
      available: true
      tool: "Vitest"
    e2e:
      available: true
      tool: "Playwright"
  coverage:
    available: false
    command: ""
  quality_tools:
    linter:
      available: false
      command: ""
    type_checker:
      available: true
      command: "npx tsc --noEmit"
    formatter:
      available: false
      command: ""
  markdown: |
    ## Testing Capabilities

    **Strict TDD Mode**: Enabled
    **Detected**: 2026-07-14

    ### Test Runner

    - Command: `npx vitest run`
    - Framework: Vitest

    ### Test Layers

    | Layer       | Available | Tool        |
    | ----------- | --------- | ----------- |
    | Unit        | ✅        | Vitest      |
    | Integration | ✅        | Vitest      |
    | E2E         | ✅        | Playwright  |

    ### Coverage

    - Available: ❌
    - Command: —

    ### Quality Tools

    | Tool         | Available | Command        |
    | ------------ | --------- | -------------- |
    | Linter       | ❌        | —              |
    | Type checker | ✅        | `npx tsc --noEmit` |
    | Formatter    | ❌        | —              |
